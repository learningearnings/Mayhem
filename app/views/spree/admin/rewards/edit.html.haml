%h1 Edit Product
.container
  = form_for :product, :url => { :action => "update" }, :html => { :class => 'form-horizontal', :method => 'put' } do |f|
    = render 'form', :f => f
  = link_to "cancel", admin_rewards_path
