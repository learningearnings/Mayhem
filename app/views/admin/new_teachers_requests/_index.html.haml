.panel
  .panel_contents
    %table.table
      %thead
        %tr
          %th Name
          %th Email
          %th Username
          %th DOB
          %th Gender
          %th Grade
          %th School
          %th Approve
          %th Delete
      %tbody
        %tr
          - @teachers.each do |teacher|
            %td= teacher.name
            %td= teacher.email
            %td= teacher.username
            %td= teacher.dob
            %td= teacher.gender
            %td= teacher.grade
            %td= teacher.schools.first.name
            %td= link_to "Approve", admin_approve_teacher_path(teacher)
            %td= link_to "Delete", admin_deny_teacher_path(teacher), :confirm => true


