.auctions-content
  %h2 Auctions
  .span8
    .auctions  
      - if auctions.any?
        %p Here are the currently-open auctions.
        = render auctions
      - else
        %p There are no auctions currently open, sorry.
