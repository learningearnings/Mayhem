
.auctions-content
  .span8
    .auctions
      %ul.action_buttons
        %li= link_to "Back to Auctions", auctions_path, class: 'btn awesome-button'
      .auction-item
        = render auction

        = form_for bid do |f|
          = text_field_tag :amount
          = hidden_field_tag :auction_id, auction.id
          = f.submit "Make bid", class: 'btn awesome-button'

