%h2 Classroom: #{@classroom.name}
%ul.action_buttons
  %li= button_to "Back", classroom_path(@classroom), class: 'btn awesome-button'

= form_for @classroom do |f|
  = f.text_field :name
  = f.submit
 
