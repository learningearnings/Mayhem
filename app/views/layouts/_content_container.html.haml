.messages-container
  = render "layouts/flashes"
  - if respond_to? :breadcrumbs
    = breadcrumbs(@taxon)
- if (sidebar = yield :sidebar).empty?
  .span12.content-section= yield
  
- else
  .span3= sidebar
  .span9.content-section= yield
