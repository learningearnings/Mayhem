.resp-masthead.noprint
  .resp-logo
    = link_to(image_tag('common/le_logo.png'), root_path)
  .resp-login-or-join
    = render 'layouts/resp_login_or_join'
.resp-nav-bar.noprint
  = render "layouts/navbar"
